import { Box } from "@mui/material";

export default function Home() {
  return (
    <Box sx={{ position: "fixed", width: "100vw", height: "100vh", backgroundColor: "background.default" }}>

    </Box>
  );
}